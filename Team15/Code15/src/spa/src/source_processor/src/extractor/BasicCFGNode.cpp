#include "../../include/extractor/BasicCFGNode.h"


int x = 1
int a = 2
while (true) { //isParent, set ppl below to child
    x = 1

    if (something) {
        //this guy
    }
    if (a) then {
        // and this
    } else {
        // and this
    }
    x = 2
}

stack parentStack = while

//do parents first
if (same parent && nesting level) follows

